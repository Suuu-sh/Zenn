# Intro

This is a short sample chapter for a private book.

## Notes

- Set `published: true` in `config.yaml` when you are ready to publish.
- Add more chapters under `chapters/` and list them in `config.yaml`.
